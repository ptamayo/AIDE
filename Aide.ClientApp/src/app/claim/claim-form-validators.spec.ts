import { ClaimFormValidators } from './claim-form-validators';

describe('ClaimFormValidators', () => {
  it('should create an instance', () => {
    expect(new ClaimFormValidators()).toBeTruthy();
  });
});
