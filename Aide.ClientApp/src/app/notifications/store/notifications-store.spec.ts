import { NotificationsStore } from './notifications-store';

describe('NotificationsStore', () => {
  it('should create an instance', () => {
    expect(new NotificationsStore()).toBeTruthy();
  });
});
